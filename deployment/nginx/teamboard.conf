server {
    listen 80;
    server_name example.com www.example.com;

    root /var/www/teamboard/frontend/dist;
    index index.html;

    client_max_body_size 20m;

    location /api/ {
        proxy_pass http://127.0.0.1:8000/api/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /admin/ {
        proxy_pass http://127.0.0.1:8000/admin/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /static/ {
        alias /var/www/teamboard/backend/staticfiles/;
    }

    # JIVO Gate app, embedded in Team Board via iframe and mounted at /gate/.
    location /gate/static/ {
        alias /var/www/teamboard/click/staticfiles/;
    }

    location /gate/ {
        # Trailing slash strips the /gate/ prefix before proxying to gunicorn,
        # so Django still serves its routes at the root (/, /api/..., /admin/...).
        proxy_pass http://127.0.0.1:8731/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}
