VITE_API_BASE=/api
# Where the embedded JIVO Gate app is served. In production it is mounted on the
# same domain at /gate/ (nginx -> gate gunicorn), which is also the default.
# For local dev the gate runs on its own port: VITE_GATE_URL=http://127.0.0.1:8731/
VITE_GATE_URL=/gate/
